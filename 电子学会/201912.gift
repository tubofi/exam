


::1:: 假设a\=20，b\=3，那么a or b的结果是（） {
    =20
    ~0
    ~1
    ~3
}

::2:: 假设a\=2,b\=3,那么a-b*b的值是（ ） {
    =-7
    ~-2
    ~-3
    ~-11
}

::3:: 下面哪一段代码是绘制一个圆 {
    =circle(50)
    ~circle(50,steps=3)
    ~circle(50,180)
    ~circle(50,0)
}

::4:: 下列Turtle库中画笔属性说法错误的是： {
    =turtle.distance()：设置画笔移动距离
    ~turtle.speed()：设置画笔移动速度；
    ~turtle.pencolor()：设置画笔的颜色；
    ~turtle.pensize()：设置画笔的宽度；
}


